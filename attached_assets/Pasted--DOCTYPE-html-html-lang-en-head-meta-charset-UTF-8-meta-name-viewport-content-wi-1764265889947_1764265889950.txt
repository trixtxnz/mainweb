<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ideas for website</title>
  <h2 align center>ideas for the website</h2>
  <h4 align center>❌ means its not done yet</h4>
<style>
  .done {
    text-decoration: line-through;
    color: #888;
    opacity: 0.7;
  }
</style>
  <input type="text"id="taskInput" placeholder="enter a new idea...">
<button onclick="addTask()">Add idea</button>
<ul id="taskList">
  </ul>
<script>
    function createDeleteButton(listItem) {
        const deleteBtn = document.createElement('span');
        deleteBtn.textContent = '❌'; // Or 'X'
        deleteBtn.classList.add('delete-btn');

        // INPUT (Interaction): Clicking the 'X' button
        deleteBtn.addEventListener('click', function(event) {
            // Prevent the 'click' from propagating to the <li> element below it
            event.stopPropagation(); 

            // OUTPUT (Change): Remove the <li> from the list
            listItem.remove();
        });
        return deleteBtn;
    }

    function addTask() {
        const taskInput = document.getElementById('taskInput');
        const taskText = taskInput.value.trim();

        // Check if the input is not empty
        if (taskText !== "") {
            const taskList = document.getElementById('taskList');

            // 1. Create the new <li> element
            const listItem = document.createElement('li');
            listItem.textContent = taskText;

            // --- Extension 1: Toggling Status ---
            // INPUT (Interaction): Clicking the task item
          //add back if needed
            //listItem.addEventListener('click', function() {
                // OUTPUT (Change): Toggle the 'done' class
                //this.classList.toggle('done');
            //});) 

            // --- Extension 2: Deleting Task ---
            // Create and attach the delete button
            //const deleteBtn = createDeleteButton(listItem);
            //listItem.appendChild(deleteBtn);

            // OUTPUT: Append the task to the list
            taskList.appendChild(listItem);

            // Clear input
            taskInput.value = '';
        }
    }
</script>
